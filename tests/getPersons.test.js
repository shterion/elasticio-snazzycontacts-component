const config = require('./../config/config');
const cfg = config.getEnvironment();
const getPersons = require('./../lib/triggers/getPersons');

describe('Test triggers', () => {
  test('should get persons', () => {

  });
});
